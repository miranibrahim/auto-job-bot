version: "3.9"

services:
  app:
    build: .
    container_name: job-bot
    env_file: .env
    volumes:
      - ./app:/app/app
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped